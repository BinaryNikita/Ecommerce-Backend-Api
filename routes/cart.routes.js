// import express from "express";
// import { addToCart, deleteCartItem, viewCart } from "../controller/cart.controller.js";

// const router = express.Router();


// router.post("/add-to-cart", addToCart);
// router.get("/:adminId", viewCart);
// router.delete('/:adminId/:productId', deleteCartItem);

// export default router;
